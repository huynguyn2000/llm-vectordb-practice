name: "warehouse"
version: "1.0.0"
profile: "warehouse"
model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
models:
  warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table
